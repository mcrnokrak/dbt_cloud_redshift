version: 2

sources:
  - name: jaffle_shop
    database: training_dev
    schema: mcrnokrak
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: orders
        columns:
          - name: id
            tests:
              - unique              
              - not_null